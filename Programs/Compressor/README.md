<html>
	<body>
		<h1 align="center">Упаковщик распаковщик данных</h1>
		<div>
			<h2>Задача</h2>
			<p>Необходимо разработать консольное приложение, которое будет преобразовывать файлы в байтовый поток и обратно</p>
			<p>Программа должна уметь работать как с одним файлом, так и с каталогом</p>
			<p>Для каталога должен присутствовать фильтр типа файлов по необходимости</p>
			<p>На выходе должен получаться всегда один файл(Коробка)</p>
			<p></p>
		</div>
		<div>
			<div>
				<h2>Конкретика</h2>
				<p>Для преобразования должены присутствовать шаблоны преобразования</p>
				<p>Например:</p>
				<p>Для каждого порожденного файла, должны присутствовать три сущности</p>
			</div>
			<div>
				<h3>Заголовок 48 байт</h3>
				<p>4 байта - идентификатор файла</p>
				<p>4 байта - тип коробки</p>
				<p>8 байт - пустые(дополнительные)</p>
				<p>16 байт - список ключей файлов и смещение на блок конкретного ключа</p>
				<p>8 байта - длинна всего файла, без заголовка, делить на длинну блоков, чтобы получить колличество блоков</p>
				<p>8 байт - пустые(дополнительные)</p>
			</div>
			<br/>
			<div>
				<h3>Блок 40 байт</h3>
				<p>8 байт - пустые(дополнительные)</p>
				<p>4 байта - смещение на ключ</p>
				<p>4 байта - смещение на длинну ключа</p>
				<p>8 байтов - пустые(дополнительные)</p>
				<p>4 байта - смещение на значение</p>
				<p>4 байта - смещение на длинну значение</p>
				<p>8 байтов - пустые(дополнительные)</p>
			</div>
			<br/>
			<div>
				<h3>Ресуры файла x-байт</h3>
				<p>x байт - последовательно записанные байты</p>
			</div>
		</div>
	</body>
</html>
